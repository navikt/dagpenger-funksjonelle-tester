import cucumber.api.PendingException
import io.cucumber.java8.No

class GsakSteps : No {
    init {
        Så("ny gsak knyttes til fagsak") {
            // TODO: Mock call to Sak and assert fagsak id
            throw cucumber.api.PendingException()
        }
    }
}
